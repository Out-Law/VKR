import 'package:flutter/material.dart';

class SelectedStarsCompon extends StatefulWidget {
  const SelectedStarsCompon({Key? key}) : super(key: key);

  @override
  State<SelectedStarsCompon> createState() => _SelectedStarsComponState();
}

class _SelectedStarsComponState extends State<SelectedStarsCompon> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
